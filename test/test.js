const chai = require('chai');
const mocha = require('mocha');
const expect = chai.expect

mocha.describe("validator isNumValid()", () => {
    it("will test", ()=> {
        // expect(validator.isNumValid(39)).to.be.true
    })
})